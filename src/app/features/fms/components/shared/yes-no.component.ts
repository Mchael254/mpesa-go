/**
 * This enum represents common Yes/No or boolean-like flag values from the vertical components
 * Using this enum improves code readability and prevents typos with magic strings.
 */
export enum YesNo {
  Yes = 'Y',
  No = 'N',
}
