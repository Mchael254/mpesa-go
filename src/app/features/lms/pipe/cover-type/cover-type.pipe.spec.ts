import { CoverTypePipe } from './cover-type.pipe';

describe('CoverTypePipe', () => {
  it('create an instance', () => {
    const pipe = new CoverTypePipe();
    expect(pipe).toBeTruthy();
  });
});
