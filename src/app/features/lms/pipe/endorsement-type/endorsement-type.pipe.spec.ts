import { EndorsementTypePipe } from './endorsement-type.pipe';

describe('EndorsementTypePipe', () => {
  it('create an instance', () => {
    const pipe = new EndorsementTypePipe();
    expect(pipe).toBeTruthy();
  });
});
