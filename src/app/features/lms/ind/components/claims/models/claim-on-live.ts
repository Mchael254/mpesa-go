export interface ClaimOnLiveDTO{
  co_desc: string;
  dty_description: string;
  dty_sht_desc: string;
  pod_adopted: string;
  pod_annuity_term: string;
  pod_claimed: string;
  pod_co_code: number;
  pod_code: number;
  pod_dob: string;
  pod_dty_code: number;
  pod_id_no: string;
  pod_other_names: string;
  pod_percentage: string;
  pod_purchase_amt: string;
  pod_sex: string;
  pod_surname: string;
  pod_unique_code_number: number;

}
